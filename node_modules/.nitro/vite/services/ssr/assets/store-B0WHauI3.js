import { i as TSS_SERVER_FUNCTION, r as createServerFn } from "../index.js";
import { r as getSql } from "./db-CGHu_QoS.js";
import { t as authMiddleware } from "./middleware-DWM_TbXu.js";
import { n as LOWER_48_CODES } from "./us-states-Cvw238TJ.js";
import { n as shippingCents } from "./money-B4nxYusR.js";
import { z } from "zod";
//#region node_modules/@tanstack/start-server-core/dist/esm/createServerRpc.js
var createServerRpc = (serverFnMeta, splitImportFn) => {
	const url = "/_serverFn/" + serverFnMeta.id;
	return Object.assign(splitImportFn, {
		url,
		serverFnMeta,
		[TSS_SERVER_FUNCTION]: true
	});
};
//#endregion
//#region src/lib/store.ts?tss-serverfn-split
function mapProduct(r) {
	return {
		id: r.id,
		name: r.name,
		sizeLabel: r.size_label,
		category: r.category,
		priceCents: r.price_cents,
		stock: r.stock,
		coaUrl: r.coa_url,
		active: r.active,
		sortOrder: r.sort_order
	};
}
async function ensureProfile(userId, email) {
	const sql = await getSql();
	const existing = await sql`select user_id, email, is_admin, membership_paid_at, credit_cents, legal_accepted_at from profiles where user_id = ${userId}`;
	if (existing[0]) {
		const p = existing[0];
		return {
			userId: p.user_id,
			email: p.email,
			isAdmin: p.is_admin,
			member: Boolean(p.membership_paid_at),
			creditCents: p.credit_cents,
			legalAcceptedAt: p.legal_accepted_at
		};
	}
	const isAdmin = ((await sql`select count(*)::int as c from profiles where is_admin = true`)[0]?.c ?? 0) === 0;
	const mail = email ?? "";
	await sql`insert into profiles (user_id, email, is_admin) values (${userId}, ${mail}, ${isAdmin})`;
	return {
		userId,
		email: mail,
		isAdmin,
		member: false,
		creditCents: 0,
		legalAcceptedAt: null
	};
}
async function loadSettings() {
	const sql = await getSql();
	const r = (await sql`select store_name, support_email, owner_email, shipping_cents, free_shipping_at_cents, nexapay_api_key, usdc_wallet, usdt_tron_wallet, btc_wallet, usdc_pay_wallet from store_settings where id = 1`)[0];
	if (!r) {
		await sql`insert into store_settings (id, store_name) values (1, 'Livewell42') on conflict (id) do nothing`;
		return {
			store_name: "Livewell42",
			support_email: "support@example.com",
			owner_email: "",
			shipping_cents: 1500,
			free_shipping_at_cents: 25e3,
			nexapay_api_key: "",
			usdc_wallet: "",
			usdt_tron_wallet: "",
			btc_wallet: "",
			usdc_pay_wallet: ""
		};
	}
	if (r.store_name === "Alder") {
		await sql`update store_settings set store_name = 'Livewell42' where id = 1 and store_name = 'Alder'`;
		r.store_name = "Livewell42";
	}
	return r;
}
function publicize(s) {
	return {
		storeName: s.store_name,
		supportEmail: s.support_email,
		shippingCents: s.shipping_cents,
		freeShippingAtCents: s.free_shipping_at_cents,
		usdcWallet: s.usdc_wallet,
		usdtTronWallet: s.usdt_tron_wallet,
		btcWallet: s.btc_wallet,
		usdcPayWallet: s.usdc_pay_wallet,
		nexapayConfigured: Boolean(s.nexapay_api_key)
	};
}
async function queueMail(kind, to, subject, body) {
	if (!to) return;
	await (await getSql())`insert into mail_log (kind, to_email, subject, body) values (${kind}, ${to}, ${subject}, ${body})`;
}
var getBootstrap_createServerFn_handler = createServerRpc({
	id: "3c3d9b7498266ffe776c7abe4931e7d6814d83a2cc54814d1a906c8116873164",
	name: "getBootstrap",
	filename: "src/lib/store.ts"
}, (opts) => getBootstrap.__executeServer(opts));
var getBootstrap = createServerFn({ method: "GET" }).middleware([authMiddleware]).handler(getBootstrap_createServerFn_handler, async ({ context }) => {
	const { getSessionUser } = await import("./verify.server-Cpby59_M.js");
	const session = await getSessionUser();
	const me = await ensureProfile(context.userId, session?.email ?? null);
	const settings = publicize(await loadSettings());
	let products = [];
	if ((me.member || me.isAdmin) && me.legalAcceptedAt) products = (await (await getSql())`select id, name, size_label, category, price_cents, stock, coa_url, active, sort_order from products where active = true order by sort_order, id`).map(mapProduct);
	return {
		me,
		settings,
		products
	};
});
var acceptLegal_createServerFn_handler = createServerRpc({
	id: "b7105fa4bf58115823b7cbe9542d404fd0c0211149f9537d34bb677cb4b4df5a",
	name: "acceptLegal",
	filename: "src/lib/store.ts"
}, (opts) => acceptLegal.__executeServer(opts));
var acceptLegal = createServerFn({ method: "POST" }).middleware([authMiddleware]).handler(acceptLegal_createServerFn_handler, async ({ context }) => {
	const sql = await getSql();
	await ensureProfile(context.userId, null);
	await sql`update profiles set legal_accepted_at = now() where user_id = ${context.userId}`;
	return { ok: true };
});
var payMembership_createServerFn_handler = createServerRpc({
	id: "41f7404c27f56955d8d712680f170a6d0c0454d96a0844fc3cae729fbec24ed6",
	name: "payMembership",
	filename: "src/lib/store.ts"
}, (opts) => payMembership.__executeServer(opts));
var payMembership = createServerFn({ method: "POST" }).validator(z.object({ rail: z.enum([
	"card",
	"usdt_tron",
	"btc",
	"usdc"
]) })).middleware([authMiddleware]).handler(payMembership_createServerFn_handler, async ({ context, data }) => {
	const me = await ensureProfile(context.userId, null);
	if (!me.legalAcceptedAt) throw new Error("Confirm the sign-in statements first.");
	if (me.member) return {
		ok: true,
		already: true
	};
	await (await getSql())`update profiles set membership_paid_at = now(), credit_cents = 500 where user_id = ${context.userId} and membership_paid_at is null`;
	const settings = await loadSettings();
	const to = me.email || settings.owner_email;
	await queueMail("membership", settings.owner_email || to, "New Livewell42 membership", `Member ${me.email || context.userId} paid $5 via ${data.rail}. Credit of $5 will apply to their first order.`);
	return {
		ok: true,
		already: false
	};
});
var checkoutSchema = z.object({
	items: z.array(z.object({
		productId: z.number().int(),
		qty: z.number().int().min(1).max(99)
	})).min(1),
	shipName: z.string().trim().min(1).max(80),
	shipStreet: z.string().trim().min(1).max(120),
	shipCity: z.string().trim().min(1).max(80),
	shipState: z.string().trim().length(2),
	shipZip: z.string().trim().regex(/^\d{5}(-\d{4})?$/),
	rail: z.enum([
		"card",
		"usdt_tron",
		"btc",
		"usdc"
	])
});
var placeOrder_createServerFn_handler = createServerRpc({
	id: "d52cdb5312482319bc218dc17cf2ccb1bf94eb34aab37a1df68e32d7857b07e5",
	name: "placeOrder",
	filename: "src/lib/store.ts"
}, (opts) => placeOrder.__executeServer(opts));
var placeOrder = createServerFn({ method: "POST" }).validator(checkoutSchema).middleware([authMiddleware]).handler(placeOrder_createServerFn_handler, async ({ context, data }) => {
	if (!LOWER_48_CODES.has(data.shipState)) throw new Error("We only ship to the lower 48 states.");
	const me = await ensureProfile(context.userId, null);
	if (!me.legalAcceptedAt) throw new Error("Confirm the sign-in statements first.");
	if (!me.member && !me.isAdmin) throw new Error("Membership required.");
	const sql = await getSql();
	const settings = await loadSettings();
	const ids = data.items.map((i) => i.productId);
	const products = await sql`select id, name, size_label, category, price_cents, stock, coa_url, active, sort_order from products`;
	const wanted = new Set(ids);
	const byId = new Map(products.filter((p) => wanted.has(p.id)).map((p) => [p.id, p]));
	let merchandise = 0;
	const lines = [];
	for (const item of data.items) {
		const p = byId.get(item.productId);
		if (!p || !p.active) throw new Error("An item is no longer available.");
		if (p.stock < item.qty) throw new Error(`${p.name} is sold out or low on stock.`);
		merchandise += p.price_cents * item.qty;
		lines.push({
			product: p,
			qty: item.qty
		});
	}
	const credit = Math.min(me.creditCents, merchandise);
	const ship = shippingCents(merchandise, settings.free_shipping_at_cents, settings.shipping_cents);
	const total = merchandise - credit + ship;
	const seq = await sql`select count(*)::int as c from orders`;
	const orderNumber = `LW42-${String(1001 + (seq[0]?.c ?? 0))}`;
	const orderId = (await sql`
      insert into orders (
        user_id, order_number, merchandise_cents, credit_cents, shipping_cents, total_cents,
        status, ship_name, ship_street, ship_city, ship_state, ship_zip, payment_rail, payment_ref
      ) values (
        ${context.userId}, ${orderNumber}, ${merchandise}, ${credit}, ${ship}, ${total},
        'paid', ${data.shipName}, ${data.shipStreet}, ${data.shipCity}, ${data.shipState}, ${data.shipZip},
        ${data.rail}, ${`demo-${Date.now()}`}
      ) returning id`)[0].id;
	for (const line of lines) {
		await sql`insert into order_items (order_id, product_id, name, size_label, qty, price_cents)
        values (${orderId}, ${line.product.id}, ${line.product.name}, ${line.product.size_label}, ${line.qty}, ${line.product.price_cents})`;
		await sql`update products set stock = stock - ${line.qty} where id = ${line.product.id}`;
	}
	if (credit > 0) await sql`update profiles set credit_cents = 0 where user_id = ${context.userId}`;
	const itemLines = lines.map((l) => `${l.qty} × ${l.product.name} ${l.product.size_label}`).join("\n");
	const money = (n) => `$${(n / 100).toFixed(2)}`;
	const body = [
		`Order ${orderNumber}`,
		itemLines,
		`Ship to: ${data.shipName}, ${data.shipStreet}, ${data.shipCity}, ${data.shipState} ${data.shipZip}`,
		`Merchandise ${money(merchandise)}`,
		credit ? `Membership credit -${money(credit)}` : null,
		`Shipping ${ship === 0 ? "FREE" : money(ship)}`,
		`Collected ${money(total)} via ${data.rail}`,
		`For laboratory research use only. Not for human consumption.`
	].filter(Boolean).join("\n");
	await queueMail("order-owner", settings.owner_email || me.email, `Livewell42 order ${orderNumber}`, body);
	if (me.email) await queueMail("order-customer", me.email, `Livewell42 receipt ${orderNumber}`, body);
	return {
		orderNumber,
		totalCents: total
	};
});
var listMyOrders_createServerFn_handler = createServerRpc({
	id: "daf7da1f4472cd1027e735edb884770371e0e02190fa6e52d3133d2f6beba984",
	name: "listMyOrders",
	filename: "src/lib/store.ts"
}, (opts) => listMyOrders.__executeServer(opts));
var listMyOrders = createServerFn({ method: "GET" }).middleware([authMiddleware]).handler(listMyOrders_createServerFn_handler, async ({ context }) => {
	return (await getSql())`select order_number, total_cents, status, tracking, created_at from orders where user_id = ${context.userId} order by id desc`;
});
async function requireAdmin(userId) {
	const me = await ensureProfile(userId, null);
	if (!me.isAdmin) throw new Error("Admin only.");
	return me;
}
var adminGet_createServerFn_handler = createServerRpc({
	id: "512e4f8b397549830348b5d5c540b3e4a87895a1dabbea707b6a6d1916cd87b5",
	name: "adminGet",
	filename: "src/lib/store.ts"
}, (opts) => adminGet.__executeServer(opts));
var adminGet = createServerFn({ method: "GET" }).middleware([authMiddleware]).handler(adminGet_createServerFn_handler, async ({ context }) => {
	await requireAdmin(context.userId);
	const sql = await getSql();
	const settings = await loadSettings();
	const products = await sql`select id, name, size_label, category, price_cents, stock, coa_url, active, sort_order from products order by sort_order, id`;
	const orders = await sql`select id, order_number, user_id, merchandise_cents, credit_cents, shipping_cents, total_cents, status, ship_name, ship_street, ship_city, ship_state, ship_zip, payment_rail, tracking, created_at from orders order by id desc limit 200`;
	const items = await sql`select order_id, name, size_label, qty, price_cents from order_items`;
	const members = await sql`select email, membership_paid_at, credit_cents from profiles order by created_at desc`;
	const sales = await sql`select
        count(*)::int as order_count,
        coalesce(sum(total_cents), 0)::int as ytd_cents,
        coalesce(sum(case when created_at >= date_trunc('month', now()) then total_cents else 0 end), 0)::int as mtd_cents
      from orders`;
	const mail = await sql`select id, kind, to_email, subject, created_at, body from mail_log order by id desc limit 40`;
	return {
		settings,
		products: products.map(mapProduct),
		orders,
		items,
		members,
		sales: sales[0] ?? {
			order_count: 0,
			ytd_cents: 0,
			mtd_cents: 0
		},
		mail
	};
});
var adminSaveProduct_createServerFn_handler = createServerRpc({
	id: "e9bd5391a0bc3b42821b87419c6982db35c6d192dda841d8ef95e6bc6b28eb27",
	name: "adminSaveProduct",
	filename: "src/lib/store.ts"
}, (opts) => adminSaveProduct.__executeServer(opts));
var adminSaveProduct = createServerFn({ method: "POST" }).validator(z.object({
	id: z.number().int().optional(),
	name: z.string().trim().min(1).max(80),
	sizeLabel: z.string().trim().max(40),
	category: z.enum(["peptide", "bac_water"]),
	priceDollars: z.string().trim(),
	stock: z.number().int().min(0),
	coaUrl: z.string().trim().max(500),
	active: z.boolean()
})).middleware([authMiddleware]).handler(adminSaveProduct_createServerFn_handler, async ({ context, data }) => {
	await requireAdmin(context.userId);
	const priceCents = Math.round(Number(data.priceDollars) * 100);
	if (!Number.isFinite(priceCents) || priceCents < 0) throw new Error("Invalid price.");
	const sql = await getSql();
	if (data.id) await sql`update products set name = ${data.name}, size_label = ${data.sizeLabel}, category = ${data.category}, price_cents = ${priceCents}, stock = ${data.stock}, coa_url = ${data.coaUrl}, active = ${data.active} where id = ${data.id}`;
	else await sql`insert into products (name, size_label, category, price_cents, stock, coa_url, active, sort_order) values (${data.name}, ${data.sizeLabel}, ${data.category}, ${priceCents}, ${data.stock}, ${data.coaUrl}, ${data.active}, 50)`;
	return { ok: true };
});
var adminSaveSettings_createServerFn_handler = createServerRpc({
	id: "8662d55d0f9c4a18986298cf1f639aef137675a54a70fd4bd9b34cc5901ab4de",
	name: "adminSaveSettings",
	filename: "src/lib/store.ts"
}, (opts) => adminSaveSettings.__executeServer(opts));
var adminSaveSettings = createServerFn({ method: "POST" }).validator(z.object({
	storeName: z.string().trim().min(1).max(40),
	supportEmail: z.string().trim().max(120),
	ownerEmail: z.string().trim().max(120),
	shippingDollars: z.string().trim(),
	freeAtDollars: z.string().trim(),
	nexapayApiKey: z.string().trim().max(200),
	usdcWallet: z.string().trim().max(200),
	usdtTronWallet: z.string().trim().max(200),
	btcWallet: z.string().trim().max(200),
	usdcPayWallet: z.string().trim().max(200)
})).middleware([authMiddleware]).handler(adminSaveSettings_createServerFn_handler, async ({ context, data }) => {
	await requireAdmin(context.userId);
	const shipping = Math.round(Number(data.shippingDollars) * 100);
	const freeAt = Math.round(Number(data.freeAtDollars) * 100);
	await (await getSql())`update store_settings set
      store_name = ${data.storeName},
      support_email = ${data.supportEmail},
      owner_email = ${data.ownerEmail},
      shipping_cents = ${shipping},
      free_shipping_at_cents = ${freeAt},
      nexapay_api_key = ${data.nexapayApiKey},
      usdc_wallet = ${data.usdcWallet},
      usdt_tron_wallet = ${data.usdtTronWallet},
      btc_wallet = ${data.btcWallet},
      usdc_pay_wallet = ${data.usdcPayWallet}
      where id = 1`;
	return { ok: true };
});
var adminUpdateOrder_createServerFn_handler = createServerRpc({
	id: "d3b7b53c448e34e2ae20831ac9d26c8d0307e706bede78bcad2ef271523cad7a",
	name: "adminUpdateOrder",
	filename: "src/lib/store.ts"
}, (opts) => adminUpdateOrder.__executeServer(opts));
var adminUpdateOrder = createServerFn({ method: "POST" }).validator(z.object({
	id: z.number().int(),
	status: z.enum([
		"paid",
		"packed",
		"shipped",
		"reshipped"
	]),
	tracking: z.string().trim().max(80),
	reshipNote: z.string().trim().max(200).optional()
})).middleware([authMiddleware]).handler(adminUpdateOrder_createServerFn_handler, async ({ context, data }) => {
	await requireAdmin(context.userId);
	const sql = await getSql();
	await sql`update orders set status = ${data.status}, tracking = ${data.tracking}, reship_note = ${data.reshipNote ?? ""} where id = ${data.id}`;
	if (data.status === "shipped" && data.tracking) {
		const order = await sql`select order_number, user_id from orders where id = ${data.id}`;
		const profile = await sql`select email from profiles where user_id = ${order[0]?.user_id ?? ""}`;
		if (profile[0]?.email) await queueMail("shipped", profile[0].email, `Shipped ${order[0].order_number}`, `Tracking: ${data.tracking}\nFor laboratory research use only.`);
	}
	return { ok: true };
});
var adminSalesCsv_createServerFn_handler = createServerRpc({
	id: "f2b124e566eb207695960765c79e9ceece9f371548ce0fc2bd53129a7a67f84c",
	name: "adminSalesCsv",
	filename: "src/lib/store.ts"
}, (opts) => adminSalesCsv.__executeServer(opts));
var adminSalesCsv = createServerFn({ method: "GET" }).middleware([authMiddleware]).handler(adminSalesCsv_createServerFn_handler, async ({ context }) => {
	await requireAdmin(context.userId);
	return ["date,order,merchandise,credit,shipping,collected,rail,status", ...(await (await getSql())`select created_at, order_number, merchandise_cents, credit_cents, shipping_cents, total_cents, payment_rail, status from orders order by id`).map((r) => `${r.created_at},${r.order_number},${(r.merchandise_cents / 100).toFixed(2)},${(r.credit_cents / 100).toFixed(2)},${(r.shipping_cents / 100).toFixed(2)},${(r.total_cents / 100).toFixed(2)},${r.payment_rail},${r.status}`)].join("\n");
});
//#endregion
export { acceptLegal_createServerFn_handler, adminGet_createServerFn_handler, adminSalesCsv_createServerFn_handler, adminSaveProduct_createServerFn_handler, adminSaveSettings_createServerFn_handler, adminUpdateOrder_createServerFn_handler, getBootstrap_createServerFn_handler, listMyOrders_createServerFn_handler, payMembership_createServerFn_handler, placeOrder_createServerFn_handler };
